import 'package:flutter/material.dart';

class ResultAssessmentStaff extends StatelessWidget {
  const ResultAssessmentStaff({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Kết quả đánh giá cấp nhân viên"),
      ),
    );
  }
}
