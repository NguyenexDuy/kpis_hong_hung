import 'package:flutter/material.dart';

class SeeSuperiorsReviews extends StatelessWidget {
  const SeeSuperiorsReviews({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text("Xem cấp trên đánh giá"),
      ),
    );
  }
}
