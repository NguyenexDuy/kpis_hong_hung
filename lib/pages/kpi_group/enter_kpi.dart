import 'package:flutter/material.dart';

class EnterKpi extends StatelessWidget {
  const EnterKpi({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Nhập số liệu KPI cho Khoa/Phòng"),
      ),
    );
  }
}
