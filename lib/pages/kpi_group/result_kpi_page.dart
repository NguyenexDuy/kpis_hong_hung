import 'package:flutter/material.dart';

class ResultKpi extends StatelessWidget {
  const ResultKpi({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Kết quả KPI Khoa/Phòng phụ trách"),
      ),
    );
  }
}
