import 'package:flutter/material.dart';

class SeeKpiRoomPage extends StatelessWidget {
  const SeeKpiRoomPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text("Xem chỉ số KPI các Khoa/Phòng"),
      ),
    );
  }
}
